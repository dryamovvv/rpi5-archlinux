[options]
Architecture = aarch64
SigLevel = Never
XferCommand = /usr/bin/aria2c --allow-overwrite=true --continue=true --file-allocation=none --log-level=error --max-tries=2 --max-connection-per-server=5 --max-file-not-found=5 --min-split-size=5M --no-conf --remote-time=true --summary-interval=60 --timeout=5 --dir=/ --out %o %u

[core]
Server = http://os.archlinuxarm.org/$arch/$repo
[extra]
Server = http://os.archlinuxarm.org/$arch/$repo
[community]
Server = http://os.archlinuxarm.org/$arch/$repo
[alarm]
Server = http://os.archlinuxarm.org/$arch/$repo
[aur]
Server = http://os.archlinuxarm.org/$arch/$repo
